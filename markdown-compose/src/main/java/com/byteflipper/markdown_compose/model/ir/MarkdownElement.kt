package com.byteflipper.markdown_compose.model.ir

/**
 * Base interface for all elements in the Markdown Intermediate Representation (IR).
 * Represents a node in the abstract syntax tree of a Markdown document.
 */
interface MarkdownElement {
    // Common properties or methods for all Markdown elements can be added here later if needed.
}
